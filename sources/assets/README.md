# Source Assets

Local screenshots, PDFs, images, and other attachments can live here when they are safe to store in the repository.

For sensitive or external files, create a source manifest in `sources/` instead of copying the asset.
